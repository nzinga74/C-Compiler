Compilar o Projeto:

1 Para compilar o nosso compilador precisamos ter o compilador de C (GCC) instalado.
2 Abrir o terminal para linux ou cmd para windows na pasta raiz do projecto

3 escrever o comando g++ -o compiler ./compiler.cpp e executar

Para executar o compiler devemos seguir os seguintes passos:
Usando o terminal executamos compiler <nome_arquivo>, em que nome do arquivo é o arquivo .c que pretendemos executar.
